export class Note {

  constructor(title: string, date: string, body: string){
    this.title = title,
    this.date = date,
    this.body = body
  }

  title: string;
  date: string;
  body: string;
}
